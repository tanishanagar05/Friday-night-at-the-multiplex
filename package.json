{
  "name": "friday-night-at-the-multiplex",
  "version": "1.0.0",
  "private": true,
  "description": "Exact-paisa cinema ticket pricing engine and booking counter API",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}